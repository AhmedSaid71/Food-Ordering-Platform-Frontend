import { Login as LoginForm } from "@/components";

const Login = () => {
  return (
    <section className="container flex justify-center w-full items-center min-h-[50vh]">
      <LoginForm />
    </section>
  );
};

export default Login;
